#include "FileManager.h"

void FileManager::saveShapesToFile(const std::vector<Shape*>& shapes, const std::string& filename) {
    // Implement later
}

void FileManager::loadShapesFromFile(std::vector<Shape*>& shapes, const std::string& filename) {
    // Implement later
}

#include "MenuHandler.h"
#include "utils.h"

void setupMenus(HWND hwnd) {
    // Create menus (File, Color, Algorithm, etc.)
}

void handleMenuCommand(HWND hwnd, WPARAM wp) {
    // Handle menu choices (set color, algorithm, etc.)
}
